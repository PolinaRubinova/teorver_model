import java.util.Random;

class model_12_7 {

    // Случайное время простоя радиоэлектронной аппаратуры
    // в ряде случаев имеет плотность вероятности ... ,
    // где M = lg e = 0.4343 логарифмически нормальный закон распределения).

    // Найти: a) моду распределения при x0 = 1 и b = корень из 5M;
    // б) функцию распределения.

    private static Random random = new Random();

    private static int newDigit () { return random.nextInt(10); }

    static int main() {

        return 0;
    }
}